import { SortstringPipe } from './sortstring.pipe';

describe('SortstringPipe', () => {
  it('create an instance', () => {
    const pipe = new SortstringPipe();
    expect(pipe).toBeTruthy();
  });
});
